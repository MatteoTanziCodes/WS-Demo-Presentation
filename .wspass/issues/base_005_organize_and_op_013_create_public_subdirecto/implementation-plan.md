# Create /public subdirectory layout and naming conventions

Create the /public/images and /public/animations subdirectories. Add a README.md documenting the naming convention (kebab-case, section prefix, no video files) and the expected file types (WebP/AVIF for photos, CSS/JS for animations). Include a .gitkeep in each empty folder.

## Acceptance Criteria
- /public/images and /public/animations directories exist
- README.md in /public documents naming convention and allowed file types
- README explicitly states no video files are permitted
- All placeholder files use kebab-case with section prefix pattern

## Relevant Components
- pass-public-assets

## Quality Commands
- Lint: n/a
- Typecheck: n/a
- Test (changed): n/a
- Test (critical): n/a
- Coverage: n/a